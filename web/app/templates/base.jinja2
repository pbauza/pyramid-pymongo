<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>{{ title or "Experiments" }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap 5 CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      background-color: #f8f9fa;
    }
    header.navbar {
      background-color: #0d6efd;
    }
    header a.nav-link,
    header span.navbar-brand {
      color: #fff !important;
    }
    header a.nav-link.active {
      font-weight: 600;
      text-decoration: underline;
    }
    .container-main {
      max-width: 900px;
      margin-top: 2rem;
    }
    .error {
      background-color: #f8d7da;
      border-color: #f5c2c7;
      color: #842029;
      padding: 0.75rem 1rem;
      border-radius: 0.375rem;
    }
    footer {
      text-align: center;
      margin-top: 3rem;
      color: #6c757d;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <header class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <span class="navbar-brand fw-bold">Experiments</span>
      <nav class="navbar-nav">
        <a class="nav-link {% if request.matched_route.name == 'home' %}active{% endif %}"
           href="{{ request.route_path('home') }}">New submission</a>
        <a class="nav-link {% if request.matched_route.name == 'list' %}active{% endif %}"
           href="{{ request.route_path('list') }}">Submissions</a>
      </nav>
    </div>
  </header>

  <main class="container container-main">
    {% block content %}{% endblock %}
  </main>

  <footer>
    <hr>
    <p>© {{ 2025 }} Experiment Manager</p>
  </footer>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
