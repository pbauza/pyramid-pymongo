<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{ title or "Experiments" }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap 5 -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <style>
      body {
        background-color: #f8f9fa;
        font-family: "Inter", system-ui, sans-serif;
      }
      header.navbar {
        background-color: #007bff;
      }
      header .navbar-brand,
      header .nav-link {
        color: white !important;
        font-weight: 500;
      }
      main {
        margin-top: 3rem;
        margin-bottom: 3rem;
      }
      footer {
        font-size: 0.9rem;
        color: #777;
        text-align: center;
        margin-top: 3rem;
      }
    </style>
  </head>
  <body>
    <header class="navbar navbar-expand-lg">
      <div class="container">
        <a class="navbar-brand" href="{{ request.route_path('home') }}">
          Experiments
        </a>
        <div>
          <a class="nav-link d-inline-block" href="{{ request.route_path('home') }}">
            New submission
          </a>
          <a class="nav-link d-inline-block" href="{{ request.route_path('list') }}">
            Submissions
          </a>
          {% if is_admin %}
          <a class="nav-link d-inline-block" href="{{ request.route_path('admin') }}">
            Admin
          </a>
          {% endif %}
        </div>
      </div>
    </header>

    <main class="container">
      {% block content %}{% endblock %}
    </main>

    <footer>
      &copy; 2025 Experiment Manager
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
