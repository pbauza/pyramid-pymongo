<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>{{ title or "Experiments" }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    /* Minimal, readable styles */
    body { font-family: system-ui, sans-serif; margin: 2rem; }
    .container { max-width: 900px; margin: 0 auto; }
    header a { margin-right: 1rem; text-decoration: none; }
    form { display: grid; gap: 0.75rem; }
    label { font-weight: 600; }
    input, textarea { padding: 0.5rem; font-size: 1rem; }
    .row { display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; }
    .actions { margin-top: 1rem; }
    .btn { padding: 0.6rem 1rem; border: 1px solid #ccc; background: #f5f5f5; cursor: pointer; }
    .btn:hover { background: #eee; }
    .error { background: #ffe5e5; border: 1px solid #ff9a9a; padding: 0.75rem; }
    table { width: 100%; border-collapse: collapse; margin-top: 1rem; }
    th, td { border: 1px solid #ddd; padding: 0.5rem; }
    th { background: #fafafa; }
    small.mono { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <a href="{{ request.route_url('home') }}">New submission</a>
      <a href="{{ request.route_url('list') }}">Submissions</a>
    </header>
    <hr />
    {% block content %}{% endblock %}
  </div>
</body>
</html>
